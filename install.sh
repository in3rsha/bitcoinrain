# link tmuxinator start config to main tmuxinator folder (can start this project from anywhere then)
mkdir -p ~/.config/tmuxinator
ln -svf ~/inersha/projects/bitcoinrain/.tmuxinator.yml ~/.config/tmuxinator/bitcoinrain.yml
